{
  "name": "parse",
  "version": "1.0.0",
  "description": "parse",
  "main": "./index.js",
  "scripts": {
    "build": "rm -rf ./dist && babel --presets es2015,react -w --stage 0 src --out-dir dist"
  },
  "keywords": [
    "parse"
  ],
  "author": "HsuTing",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.8.0",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-react": "^6.5.0"
  },
  "dependencies": {
    "jsdom": "^9.3.0"
  }
}
